# 야구는 9회말 2아웃부터 — 프로젝트 컨텍스트

KBO 배경의 **야구선수 커리어 시뮬레이션** 웹게임. 고교 3학년부터 드래프트·프로·FA·은퇴까지.
참고 사이트 `slbcareer.com`에서 출발했지만 시뮬레이션 깊이는 그쪽보다 훨씬 앞서 있다.

```bash
npm run dev     # http://localhost:3000
npm run build   # 타입체크 포함
npm run lint
```

## 작업할 때 반드시 지킬 것

### 1. 밸런스를 바꾸면 반드시 측정한다
숫자를 눈대중으로 고치지 않는다. `scripts/`에 검증 스크립트가 갖춰져 있다.

```bash
npx tsx scripts/audit.ts      # ★ 288개 커리어 전 경로 스트레스 테스트 (필수)
npx tsx scripts/balance.ts    # 유형별 커리어 분포·나이별 OVR
npx tsx scripts/debut.ts      # 고졸/대졸 1군 데뷔·주전 정착 나이
npx tsx scripts/features.ts   # 이벤트 체인·목표·대기록 발생률
npx tsx scripts/war.ts        # WAR가 세이버메트릭스 표준식과 맞는지
npx tsx scripts/training.ts   # 훈련 선택지별 성장량 비교
npx tsx scripts/hellcheck.ts  # 지옥 훈련이 항상 최고인지 (1500건 전수)
npx tsx scripts/textcheck.ts  # 화면 문구에 null/undefined가 박히는지 (25만건)
```

**`audit.ts`는 어떤 변경 후에도 돌린다.** 예외 0 · 무한정지 0 · 이상값 0 · 전 단계 도달이 기준선이다.

### 2. 스케일을 건드리면 기준선을 전부 같이 옮긴다

능력치는 **0~120**, 리그 평균은 `LEAGUE_AVG_ABILITY = 70.5` (`rng.ts`).
**생성 시 능력치(`player.ts`의 `base`/`room`)를 건드리면 OVR 분포 전체가 움직인다.**
아래 목록은 OVR 절대값을 문턱으로 쓰는 곳 전부다 — 하나라도 빠뜨리면 조용히 어긋난다.

#### 스케일의 뿌리
| 위치 | 무엇 |
|---|---|
| `rng.ts` | `LEAGUE_AVG_ABILITY` · `n50()` — 성적 계산의 기준점 |
| `player.ts` | `rollCandidate()`의 `base`(현재 52) · `room` — **생성 분포 자체** |
| `player.ts` | `ABILITY_MAX`(120) |

#### OVR 절대값을 문턱으로 쓰는 곳
| 위치 | 무엇 | 현재 값 |
|---|---|---|
| `player.ts` | `gradeOf()` 등급 문턱 | S 95 / A 87 / B 79 / C 69 / D 55 |
| `career.ts` | `assignRole()`의 `bar` | 73 기준 ± 팀 성향 |
| `career.ts` | `shouldForceRetire()`의 `releaseBar` | 나이별 66~82 |
| `career.ts` | `retirementAdvice()` | `ovr < 72` (36세 이상) |
| `career.ts` | `marketValue()` | `(ovr − 72) × 1200` |
| `career.ts` | `makeTransferTargets()` | `merit (ovr − 76)` · `contendFit (ovr − 78)` · `release (ovr − 80)` |
| **`career.ts`** | **`draftScore()`** | `ovr × 0.55` · `potWeight` · `upside` |
| **`career.ts`** | **`runDraft()` 지명 순위 밴드** | 91 / 84 / 78 / 72 / 66 / 59 |
| **`career.ts`** | **`draftForecast()` 표기 밴드 + `odds`** | 같은 6단계 · `(score − 54) / 34` |
| `national.ts` | 대회별 `bar` (대표팀 발탁) | AG 76 · P12 80 · OLY 81 · WBC 84 |
| `national.ts` | `isCalledUp()`의 `overall(p) − 78` | — |
| `national.ts` | `sangmuOdds()`의 `(ovr − 72)` | — |
| `sim.ts` | `LEVEL_ADJ` (레벨별 난이도) | HS 25 · COLLEGE 15 · MINOR 8 |
| `amateur.ts` | `FIELD_LEVEL` (아마추어 상대 수준) | HS 47 · COLLEGE 60 |
| `events.ts` | 포지션 전환 조건 `overall(p) < 78` | — |

#### 바꿨으면 반드시 돌린다
```bash
npx tsx scripts/audit.ts       # 예외·정지·이상값
npx tsx scripts/balance.ts     # 전성기 OVR (기준선 79~82)
npx tsx scripts/statdist.ts    # 리그 타율·ERA 인플레 확인
npx tsx scripts/draft.ts       # 고졸/대졸 지명 순위
npx tsx scripts/draftscore.ts  # 성적 ↔ 예상 라운드가 붙어 있는지
npx tsx scripts/debut.ts       # 1군 데뷔·주전 정착 나이
```

**실제로 겪은 사고 세 번**
- 리그 타율이 `.293`까지 뜀 — `LEVEL_ADJ`를 안 옮김
- 대표팀이 한 번도 안 뽑힘 — 대회 `bar`를 안 옮김
- **생성 OVR을 44→52로 올리고 드래프트 밴드를 안 옮겨, OVR 45·WAR −0.2가 2~3라운드로 지명됨**
  (점수가 통째로 +4.4 올라 전원이 한 단계씩 상위로 밀렸다)

### 3. 새 phase·action을 만들면 스크립트도 가르친다
`HALF_REVIEW`처럼 **한 단계에서 액션이 두 번** 필요한 곳은 `audit.ts`의
`STAY_OK`에도 넣는다. 안 넣으면 정상 흐름이 무한루프로 잡힌다. (실제로 겪음)
현재 `STAY_OK`: `STOVE` `ALL_STAR` `HALF_REVIEW` `PATH_CHOICE` `SEASON_END`

`scripts/autoplay.ts`와 `scripts/audit.ts`의 switch에 추가하지 않으면
자동 플레이가 `default: RETIRE`로 빠져 **측정값이 전부 거짓이 된다.** (실제로 겪음)

### 4. 세이브 마이그레이션
`GameState`에 필드를 추가하면 **반드시** `src/lib/migrate.ts`에 기본값을 넣는다.
저장소 경계(`storage.ts`)에서 한 번 통과시키므로 여기만 지키면 구버전 세이브가 산다.

### 5. UI 스케일 · 한 열 레이아웃

화면 폭은 `globals.css`의 `.app-shell`(460px) 하나로 잡는다.
**PC에서도 모바일 기기 한 대만 가운데 세운다** — 바깥은 무대(`--stage`)다.
`.app-shell`에 `overflow: hidden`을 쓰면 스크롤 컨테이너가 되어 **sticky 헤더가
떨어져 나간다.** `overflow: clip`을 쓴다. (실제로 겪음)

야구 느낌은 표면으로 낸다 — `.pinstripe`(유니폼) · `.seam-line`(공 실밥) ·
`.scoreboard`/`.scoreboard-num`(전광판) · `.scorebook`(기록지 격자) ·
`.jersey`(등번호) · `.ballpark`(구장 실루엣 — 중계 배경).
탭 바는 더그아웃(어두운 구단색), 기록 표는 스코어북 격자 위에 올린다.
**숫자만** 콘덴스드 서체(`.num` / `--font-scoreboard`, Oswald)로 바꾼다 —
한글 글리프가 없으므로 본문 서체를 반드시 뒤에 깔아둔다.
능력치 막대는 전광판 눈금(어두운 트랙 + 3px 칸)으로 그린다.

참고용 구역은 `<Fold>`로 접는다 — 상세 기록 · 최근 소식 · 아마추어/2군 표.

**중계 진행 바는 `i`가 아니라 `Math.min(i, steps.length - 1)`로 그린다.**
`i`는 마지막 단계를 지나 한 칸 더 올라간다(정리하는 중). 그 값을 key에 쓰면
마지막 카드가 다시 마운트돼 **같은 장면이 두 번 뜬 것처럼 보인다.**
로딩 화면이 떠 있는 동안에는 칸을 채우지 않는다 — 첫 칸만 로딩 시간까지
얹혀 혼자 느리게 찬다. (둘 다 실제로 겪음)

**오버레이는 페이지 최상위에서 그린다.** `.stage`의 transform 애니메이션이
stacking context를 만들어, 그 안에서 그린 `fixed`는 sticky 탭 바보다
아래로 깔린다. (실제로 겪음)
`.card`는 Tailwind 유틸리티보다 늦게 선언돼 우선하므로
`className="card bg-[...]"`로는 배경을 못 바꾼다 — `style`로 준다.

**브라우저 저장소를 렌더에 쓰면 hydration이 깨진다.** 초기 state에서 읽으면
서버(항상 기본값)와 값이 달라지고, effect에서 setState하면 연쇄 렌더가 된다.
`useSyncExternalStore(구독, 클라이언트값, 서버값)`로 마운트 여부를 받는다.

넓다고 정보를 옆으로 늘어놓으면 기기마다 다른 게임이 되고, 오버레이·중계·카드 연출을
두 벌로 만들어야 한다. `lg:` 분기를 새로 만들지 않는다.

능력치 막대는 `ABILITY_MAX(120)` 기준으로 그린다. 0~100 기준으로 그리면
98이 꽉 찬 것처럼 보여 "상한이 99인가?"라는 오해가 생긴다. (실제로 겪음)

### 6. 보직 판정은 `roles.ts`만 쓴다
`role.includes("선발")` 같은 문자열 비교를 새로 쓰지 않는다.
`roleTier()` · `isRotationRole()` · `isFranchiseRole()` · `isEverydayRole()`을 쓴다.
`에이스`에는 '선발'이라는 글자가 없어서, 문자열로 비교하면 선발이 불펜으로 계산된다. (실제로 겪음)

### 7. 구간을 나눠 시뮬레이션할 때는 누적 차분
월별·시리즈별로 `share`를 따로 반올림하면 합계가 어긋난다(시즌이 145경기가 됐다).
`SimInput.cume: [from, to]`로 넘기면 `allocate()`가 누적값의 차이로 배분해 총합이 정확히 맞는다.
표본이 작은 구간(`pa < 60`, `ip < 25`)은 기댓값 반올림 대신 **추첨**한다 —
안 그러면 한 경기 기록이 언제나 똑같이 나온다. (실제로 겪음: 국제대회 타율이 전부 `.250`)

## 구조

게임 로직(`src/lib/`)은 React에 의존하지 않는 순수 TypeScript다.

| 파일 | 역할 |
|---|---|
| `types.ts` | 전역 타입. `GameState`가 세이브 한 덩어리 |
| `rng.ts` | 시드 난수(mulberry32) + `LEAGUE_AVG_ABILITY` |
| `player.ts` | 생성·유형·특성·투구폼·에이징·성장·훈련·스카우팅 |
| `sim.ts` | 시즌 시뮬레이션(타자/투수), 올스타전, 수상 판정 |
| `career.ts` | **상태 머신** `advance(state, action)` — 가장 큰 파일 |
| `events.ts` | 이벤트 체인 (선택 → 1~2시즌 뒤 결과) |
| `records.ts` | 구단 목표·대기록·통산 이정표·역대 순위·별명 |
| `amateur.ts` | 고교·대학 전국대회 |
| `national.ts` | 국제대회·대표팀·병역 |
| `military.ts` | **복무 방침·부대 소식** — 야구 밖의 18개월 |
| `postseason.ts` | 가을야구 (시리즈별 개인 기록 포함) |
| `roles.ts` | **1군 입지 티어** — 보직 판정의 단일 소스 |
| `resolve.ts` | **올해의 각오** — 한 시즌 내내 걸리는 4택 |
| `rivals.ts` | **드래프트 동기 6인** — 같이 늙고, 타이틀을 다투고, 먼저 은퇴한다 |
| `form.ts` | **월별 컨디션 7단계** — 배지·문구·이달의 선수 판정의 단일 소스 |
| `clutch.ts` | **승부처** — 반기 시작 때 고르고 중계에서 결과가 열린다 |
| `legacy.ts` | 은퇴 후 — 명예의 전당 헌액 투표, 진로 7종 |
| `flavor.ts` | 시즌 총평 헤드라인, 팬 반응 |
| `migrate.ts` | 구버전 세이브 → 현재 구조 |
| `storage.ts` | localStorage + `useSyncExternalStore` |

UI는 `src/app/page.tsx`(홈) · `create/page.tsx`(4단계 생성) · `play/[id]/page.tsx`(본편 4탭) ·
`components/broadcast.tsx`(월별 중계 연출).

## 시즌 흐름

```
고교 3학년 → 진로(드래프트/대학 2년×2) → 신인 드래프트
  ┌─────────────────── 매 시즌 ───────────────────┐
  [입영 통지] → 스프링캠프(훈련) → [대표팀 발탁]
    → [WBC 3월] → 전반기(월별)   ※ 전역하는 해는 스프링캠프·전반기를 건너뛰고 올스타에서 합류
    → 올스타 브레이크 [올스타 선정 → 올스타전] [올림픽 7월] [+트레이드]
    → 후반기(월별) [아시안게임 9월] → 가을야구 [프리미어12 11월] → 시즌 총평
    → [커리어 갈림길] → 연봉협상 or FA → 스토브리그(이적 신청)
  └───────────────────────────────────────────────┘
                    → [은퇴 권고(거부 가능)] → 은퇴 → 진로 선택 → 명예의 전당 투표
```

전반기·후반기는 **월 단위로 따로 시뮬레이션**하고, 월말마다 콜업·강등을 판정한다.
국제대회는 대회마다 실제 개최 월이 달라 `Tournament.slot`(PRE/MID/LATE/POST)에 따라
네 시점 중 하나에서 치러진다.

## 현재 밸런스 기준선 (2026-09-13)

| 항목 | 값 |
|---|---|
| 랜덤 생성 | **그 단계의 항목만** 굴린다 — 한 번에 4단계를 밀면 무엇이 정해졌는지 못 보고 넘어간다. 유형을 먼저 뽑고 `RECOMMENDED_POSITIONS`에서 자리를 고른다(좌투 포수 같은 조합 0건) |
| ★ 유형은 OVR을 못 건드린다 | 유형 가중치는 **어디에 쓸지**만 정하고 총량은 건드리지 않는다. 그냥 더하면 OVR 가중치가 낮은 능력(수비 0.85 · 송구 0.5)에 몰아주는 유형이 같은 점수를 쓰고도 낮은 OVR로 출발한다 — 실측 수비형 50.6 ↔ 중장거리형 53.1. 후보 카드는 둘 다 "균형형 52"라고 적어놓고 2.5가 벌어졌고, 그 차이가 피크(79 ↔ 84) · 1군 시즌(13.5 ↔ 16.2) · 타석(413 ↔ 472)으로 **곱해져** 통산 WAR이 2.25배가 됐다. `rollCandidate`가 OVR 가중 평균을 빼서 중립화한다 (`scripts/styledrift.ts`) |
| 유형별 값 | 같은 OVR 80·같은 타석이면 유형끼리 시즌 WAR 3.5~4.2로 붙어 있다(`stylewar.ts`) — **값은 원래 맞았다.** 커리어 격차는 값이 아니라 출발 OVR에서 왔다. 중립화 뒤 통산 WAR 폭 2.25배 → **1.31배** |
| `STYLE_LEVEL`·`ROOM_LEVEL` | 중립화로 내려앉은 수준을 되돌리는 상수다. `STYLE_LEVEL`은 **생성 OVR 52**를 맞추므로 건드리면 스케일이 통째로 움직인다. `ROOM_LEVEL`은 잠재력의 폭 — **일부러 넉넉하게 뒀다**(3.6 → 피크 83.7 · 1.9 → 82.4). 커리어 내내 조금씩이라도 자라는 쪽이 재미있다는 판단이다 |
| ★ 구원 투수 레버리지 | 구원은 **승부처에서 던진다** — WAR가 그 무게를 셈에 넣는다. FanGraphs의 `(1+gmLI)/2`를 따라 **마무리 1.4 · 필승조 1.15 · 추격조 0.85**(선발 1.0). 마무리를 1.0으로 두면 같은 기량의 선발과 통산 WAR이 42 ↔ 16.5로 벌어져 명예의 전당이 사실상 막힌다 (실제로 겪음). 고친 뒤 SP 41.7 ↔ CP 21.9 ↔ RP 17.3 — 실제(오승환 ↔ 양현종 ≈ 0.56배)와 같은 크기 |
| 타자 포지션 격차 | C 59.8 ↔ DH 29.4(통산 WAR, 2.0배)는 **실제보다 오히려 작다** — FanGraphs 포지션 조정은 C(+12.5)−DH(−17.5) = 30런 = 시즌당 3.0 WAR인데 우리는 2.0이다. 손대지 않는다 |
| 후보 3인 | 완성형 OVR 57·잠재 79 / 균형형 52·84 / 원석형 47·90 (트레이드오프 축) |
| 특급 유망주 | 후보당 7% — 완성도·성장 여지를 모두 갖춘다 (셋 중 하나 나올 확률 19%) |
| 1군·2군 기록 | 오간 시즌은 `SeasonRecord.byLevel`로 나뉜다 — 통산은 `seasonsAtLevel()`로 뽑는다 |
| 아마추어 | 생성 52 → 고졸 입단 56 / 대졸 입단 68 (대학도 시즌 뒤 훈련이 있다) |
| 드래프트 | 지명은 18세(고졸)·21세(대졸)에 받고 **입단은 이듬해** (19세 / 22세) |
| 지명 순위 | 고졸 중앙 34순위 · 1라운드 19% / 대졸 중앙 17순위 · 1라운드 33% |
| 출신 학교 | 이름 해시로 전력 38~74 · 실제 명문고는 78~92 (이스터에그) |
| 학교 맞바꿈 | 강팀일수록 대회 성적↑(총점 7.7→10.5) 출전 기회↓(97타석→57타석) |
| 신인 첫 시즌 | 고졸 2군 100% / 대졸 1군 23% · 2군 75% |
| 신인 첫 풀시즌 | OPS .747 · WAR 0.6 · 최대 2.5 |
| 올해의 각오 | 매 시즌 4택 — 성적·부상·성장·한계돌파·노쇠·출장·신뢰에 **한 해 내내** 걸린다 (`resolve.ts`) |
| 각오의 균형 | 어느 하나도 항상 옳지 않다 — 평균 WAR 폭 4.5 안. 대신 **모양이 갈린다**(타이틀 도전 12.3타이틀·15.4시즌 ↔ 몸만들기 7.1타이틀·16.9시즌·은퇴OVR +4) |
| 선택의 무게 | 같은 시드에서 짝지어 본 WAR 폭 **10.2** (운 51.8 → 5.1배). 각오 도입 전엔 1.7 (34배)였다 — `agency.ts` |
| 측정 주의 | 균형 잡힌 선택지는 **평균끼리 빼면 상쇄된다.** "내 선택이 내 커리어를 바꾸는가"는 같은 시드에서 짝지어 재야 한다 |
| 훈련 | **방향 5종(타자)/4종(투수)** 선택 · 능력치는 자동 배분 (주력 1배 · 곁가지 0.5배) |
| 훈련 방향의 덮개 | **모든 능력은 적어도 한 방향에서는 오를 수 있어야 한다.** 투수수비는 어느 방향의 주력도 곁가지도 아니어서 OVR에는 들어가면서 평생 한 칸도 못 올리고 노쇠로 깎이기만 했다 — 화면은 "성장 여지"를 보여주는데 갈 길이 없었다 (`scripts/traincover.ts`) |
| 잠재력은 추정치다 | 잠재력에 닿으면 훈련 상승분이 **상한에서 통째로 잘려나간다.** 실측 27~29세에 능력치의 48%가 천장에 붙어, 훈련을 골라도 달라지는 게 없었다. 기대를 넘는 시즌은 `reviseUpside()`가 추정을 위로 고친다 — 커리어당 3.5회 · 한두세 항목 × 1~4, 막힌 항목 48% → 34% (`scripts/ceiling.ts`) |
| 재평가의 기준은 자기 자신 | **자기 최고 WAR를 넘었거나 타이틀을 땄을 때**만 연다. WAR 절대값을 기준으로 삼으면 보직이 기준을 정해버려, 마무리는 WAR 2.7이 천장이라 선발용 선을 영영 못 넘는다 (실제로 겪음: 커리어당 0.5회). 자기 최고가 기준이면 좋은 시즌이 쌓일수록 넘기 어려워져 눈덩이가 저절로 멎는다 |
| 재평가의 대가 | 후반 성장을 열면 피크 OVR이 오른다. **공짜 점심은 없다** — OVR은 능력치의 가중평균이라 뒤를 살리면 앞을 줄여야 총량이 같다 |
| 앞을 깎는 자리 | 훈련 `gain`은 **전 연령에 걸린다** — 앞만 깎으려면 `ageFactor()`의 젊은 구간(refAge ≤ 24)을 낮춰야 한다. 전성기를 되올릴 때도 같은 이유로 25~31 구간만 건드린다 |
| 성장 곡선의 모양 | 어린 나이를 깎고 전성기·30대를 올린 결과 — 19~23세 OVR 68.5 → 67.2 · 27~29세 81.8 → 82.2 · 30~32세 80.0 → 81.0 · 33~35세 77.2 → 78.5. 막힌 항목 48% → 34%. **피크 자체(82.6)와 리그 기록은 그대로**고 곡선의 모양만 바뀌었다 |
| 앞을 깎은 대가 | 고졸 1군 데뷔 20.7 → 21.0세 · **주전 정착 23.6 → 24.5세**. 실제 KBO(고졸 주전 23~25세)에 오히려 가깝다. 신인 첫 시즌 성적·은퇴 나이·노쇠 곡선은 그대로 |
| 성장 곡선을 건드렸으면 | `potcap`(피크 OVR) · `ceiling`(막힌 항목·훈련 몫) · `statdist`(리그 기록) · `debut`(데뷔·주전 정착 나이) · `rookiestat`(신인 성적) · `traingrade`(기댓값 1.0) · `hellcheck`를 **before/after로 같이** 잰다. 측정에 ±0.2 노이즈가 있으니 한 번 돌린 값으로 판단하지 않는다 |
| 지옥 훈련 | 커리어 **2회** · 성공률 ~50% · 성공 2.3배 / 실패 0.42배 (부상보다 헛수고가 위험) |
| 훈련 성과 | 같은 방향을 골라도 갈린다 — 대성공 7% ×1.7 · 잘 풀림 21% ×1.3 · 무난 43% ×1.0 · 잘 안 됨 23% ×0.7 · 헛돈 겨울 7% ×0.4 |
| 훈련 성과 기댓값 | **가중 기댓값 1.0** — 여기를 건드리면 성장 곡선 전체가 움직인다 (`traingrade.ts`) |
| 구단 색 | **실구단 색을 눌러 담은 값**이다 — 채도 평균 0.75 → 0.45. 색상(hue)은 그대로 둬 어느 팀인지는 알아본다. 헤더·탭 바·중계 배경이 화면의 절반을 덮으므로 순색(#ce0e2d · #fc4e00)은 눈이 아프다. 낮추면 빨강 네 팀이 뭉개지므로 명도로 갈라 둔다(가장 가까운 두 팀 ΔE 12.6 · 흰 글씨 대비 4.9+) |
| 구장 성향 | 실제 파크팩터를 **눌러 담은 게임용 값**이다(홈런 60% · 안타 45%). `sim.ts`가 다시 절반만 적용한다(홈경기가 절반) |
| 구장 효과 크기 | 최고↔최저 구장 사이 홈런 2.1개 · OPS 27 · 타자 WAR 0.5 · ERA 0.13 — 실제 KBO 격차(OPS 20~30)와 같은 크기. **거들 뿐 커리어를 뒤집지 않는다** (`park.ts`) |
| 희망 구단 | 드래프트 확률(순위·육성 성향에 따라 17~48%)에서 끝나지 않는다 — 못 가도 **이적·FA 시장에서 계속 따라붙고**(관심 +11, FA 후보 자리 하나 확보), 그 유니폼을 입은 해에는 신뢰·동료 +2/시즌 |
| 희망 구단 실측 | 한 시즌이라도 뛰는 커리어 84% · 1군 시즌의 38%를 거기서 보낸다 (무작위면 10%) — `wish.ts` |
| 희망 구단 주의 | 지명 실패 시 후보에서 **희망 구단을 빼야** 한다. 넣어두면 모든 순위에 1/10이 덤으로 붙어 순위별 차이가 뭉개진다 (실제로 겪음) |
| 승·패 추첨 | 승과 패를 **따로 뽑지 않는다** — 같은 경기가 승이면서 패가 돼 드물게 승+패 > 등판이 된다. 승패가 갈린 경기 수를 먼저 뽑고 그 안에서 가른다 |
| 이적 성사 | 표기 확률 = 실제 성사율 (오차 5%p 이내) · OVR 70~79 34% · 80~89 68% |
| 전성기 OVR | 커리어 최고 평균 **83.7** · 도달 28.3세 (나이별 평균은 27~28세에 80 안팎). 잠재력을 넉넉히 둔 결과다 — `ROOM_LEVEL` 참고 |
| 은퇴 | 평균 36.1세, 1군 14.2시즌 (34세부터 은퇴 권고, 거부 가능) |
| 노장 생존 | 35세 84% · 37세 30% · 39세 2.5% — 서른아홉 주전은 리그에 한둘이어야 한다 |
| 에이징 낙폭 | 같은 선수 기준 35세 −5 · 38세 −9 · 40세 −12 (실제 곡선과 일치) |
| 은퇴 권고 거부 | 그해 협상은 100% 삭감(평균 −67%) · 재협상 성공률 42%→14% |
| 시즌 기록의 보직 | **가장 오래 맡은 자리**를 적는다(`seasonRoleGames`). 시즌이 끝난 시점의 보직을 적으면, 8월에 마무리로 올라간 선수의 한 해가 통째로 "마무리"로 찍히는데 정작 숫자는 세이브 0에 홀드 19다 (실제로 겪음). `monthLines`는 **후반기가 전반기를 덮어쓰므로** 거기서 세면 반쪽만 본다 — 월마다 따로 쌓는다 |
| 섞인 줄은 모순이 아니다 | 시즌 중에 보직이 바뀌면 한 해 기록에 세이브와 홀드가 함께 남는다 — "필승조 · 세이브 23"은 마무리를 맡았다 내려온 선수의 진짜 기록이다. 걸러야 할 것은 그 보직에서 **나올 수 없는** 줄이다(마무리로 한 해를 보냈는데 세이브 0) |
| 결장·소속 표기 | **경기 수로 말한다** — 비율로 쓰면 분모가 달라 모순으로 읽힌다 |
| ★ 부상은 실제보다 가볍게 둔다 | **의도된 선택이다.** 한 달 이상 이탈 14%는 실제 KBO(주전급 30~40%)보다 가볍고, 반 시즌을 날린 이듬해 성적에 여파도 남기지 않는다(WAR 3.68 → 2.47 → 3.69). 실제와 맞추려고 올리지 말 것 — **부상이 잦으면 게임이 재미없어진다.** `nextSeasonAvailability`(이월 가동률)가 이벤트 체인에서만 쓰이고 부상에서는 안 쓰이는 것도 그래서 그대로 둔다 |
| 부상의 갈래 | 넷이다 — ① 시즌 부상(`rollInjury`, 개막 때 굴린다) ② 스프링캠프 부상(훈련 위험 5% · 지옥 2.2배) ③ 이벤트 체인(이듬해 가동률 0.75로 이월) ④ 가동률 0.6 미만이면 **개막부터 2군 말소** |
| 부상 실측 | 1군 시즌당 30%(경미 18 · 중간 9 · 심각 3) · **한 달 이상 이탈 14% · 반 시즌 이상 3.3%** · 규정타석 도달 61%. 나이별 19~26세 26% → 34~36세 40% → 37세+ 47% (`scripts/injurybal.ts`) |
| 부상의 무게 | 같은 선수 안에서 짝지어 보면 부상 시즌 WAR 3.68 → **2.47**(−1.21), **이듬해 3.69로 완전 회복**. 반 시즌 부상은 이듬해 2군행을 1% → 4%로 올린다. 심각 부상은 내구성을 영구히 깎아 이후 부상률이 21% → 38%가 된다 |
| 부상 측정 주의 | **부상 횟수로 커리어를 나누면 거꾸로 나온다** — 여섯 번 다치려면 열여섯 시즌을 뛰어야 하므로 좋은 커리어가 통째로 '부상 많음'에 몰린다(실제로 겪음: 부상 6회+ 커리어가 통산 WAR 56.4로 0~1회의 53.5보다 높게 나왔다). 시즌당 부상률로 정규화하고, 같은 선수의 앞뒤 시즌과 짝지어 잰다 |
| 부상은 달력 위에 놓는다 | 가동률을 모든 달에 곱하면 113경기 결장한 선수가 일곱 달 내내 6이닝씩 던진 걸로 찍힌다. `monthAvail`로 **연속된 달을 통째로 비운다** — 다친 시즌 0인 달 평균 2.7개 / 건강한 시즌 0개 (`injurymonth.ts`) |
| 누적 차분의 기준 | `playHalf`의 cume는 달력이 아니라 **건강 가중 누적**이다. 가중 합이 그대로라 시즌 총량은 안 변한다 |
| `kboShare`의 뜻 | **1군 등록 기간**이지 출장 경기 수가 아니다 — "뛰었습니다"로 쓰면 "113경기 결장"과 정면으로 부딪힌다 |
| 기록의 불변식 | 한 줄 안에서 숫자끼리 어긋나지 않아야 한다 — 장타 ≤ 안타 · 타수+볼넷+사구 ≤ 타석 · 승+패 ≤ 등판 · **자책 ≤ 내가 내보낸 주자(피안타+볼넷)** · ERA/WHIP/K9/AVG/OBP/SLG는 원기록에서 되계산. 짧은 구간(한 경기·한 시리즈)에서만 깨지므로 시즌 표만 봐서는 안 보인다 (`scripts/integrity.ts` 8,400줄 0건) |
| 비율은 짧은 구간에서 센다 | 자책은 비율(FIP)로 뽑고 피안타·볼넷은 따로 세므로, 한 경기 등판에서 "2명 내보내고 3자책"이 나온다. 2루타·3루타를 **같은 모수에서 따로 뽑으면** 합이 안타를 넘어 단타가 음수가 된다 — 3루타는 2루타를 뺀 나머지에서 뽑는다 |
| 타수는 0일 수 있다 | 4타석 전부 볼넷인 경기. 최소 1로 눌러두면 타수+볼넷+사구가 타석을 넘는다 — 나누는 자리(타율·장타율)를 대신 지킨다 |
| OPS 표기 | **반올림한 출루 + 반올림한 장타**로 낸다. 반올림 전 값으로 더하면 화면의 .341 + .401이 .743으로 찍혀, 눈으로 더해본 사람에게 틀린 숫자가 된다 (`mergeLines`도 같은 방식) |
| 합계는 부분의 합이다 | 가을야구·국제대회처럼 **합계와 경기별 기록이 둘 다 화면에 나오는 곳**은, 승부처를 얹은 뒤 `mergeLines`로 합계를 다시 낸다. 두 곳에 따로 더하면 갈라진다 (실제로 겪음: 자책 상한이 시리즈에만 걸려 가을야구 ERA가 시리즈합과 어긋났다) |
| 일정 문구 | KBO는 **3연전 단위**로 돌고 월요일은 쉰다 — 홈 연전은 9경기가 최대다. "홈 10연전"은 없는 일정이었다. 달별 문구 풀에서 그해 시드로 고르고, 2군은 퓨처스 전용 풀을 쓴다. 적는 것은 실제 달력에 있는 것만 — 어린이날·광복절 3연전, 장마철 더블헤더, 7/31 트레이드 마감, 9/1 확대 엔트리 |
| 값의 범위 | 능력치 1~120(정수) · 인지도/신뢰/컨디션 0~100 · 연봉 `MIN_SALARY`~`MAX_SALARY`(정수) · 등급은 OVR 문턱과 같은 말을 한다. **투수의 '컨택'처럼 안 쓰는 항목은 0이므로 `abilityKeys(kind)`로 걸러서 본다** (`scripts/valuecheck.ts`) |
| 목표 표기 ↔ 판정 | 라벨("165이닝")과 재는 선(`ip >= 165`)이 같아야 한다. id는 예전 값으로 남은 게 많아(`ip170`이 "165이닝") 눈으로는 안 보인다 (`scripts/goaltext.ts`) |
| ★ 리그 평균 기준 | **능력치 70.5 선수가 실제 KBO 리그 평균을 낸다** — 타율 .268 · 출루 .341 · 장타 .401 · OPS .742 · ERA 4.10 · WHIP 1.44 · K/9 7.2 (`leagueavg.ts`) |
| 타자 공식의 뼈대 | **절편 = 실제 리그 평균 · 기울기 = 최고 선수가 실제 1위에 닿는 값.** 절편이 낮고 기울기가 가파르면 평균 선수가 프로 같지 않다 (실제로 겪음: 평균 타자 OPS .643) |
| `lgWoba` | WAR의 기준점 — **리그 평균 능력치의 실제 wOBA**(0.331)여야 한다. 타자 공식을 바꾸면 반드시 같이 옮긴다 |
| WAR 곡선 | OVR 70 → 2.1 · 80 → 4.0 · 90 → 5.8 (세이버메트릭스 표준: 리그 평균 주전 = 2.0 WAR) |
| 리그 기록 | 주전 타율 .290 · OPS .844 · 22홈런 · WAR 3.2 / 선발 170이닝 · ERA 3.72 · 12승 |
| 올스타 기준점 | 점수식의 기준점은 **그 시절 주전의 평균**이다 — 리그 공격력을 올리고 안 옮기면 평범한 주전이 전부 올스타가 된다 (17% → 36%) |
| 승·패·세이브·홀드 | **언제나 추첨한다** — 시즌을 월로 쪼개 부르므로 경기당 0.05 같은 확률을 반올림하면 9경기 달엔 0, 10경기 달엔 1로 계단처럼 갈린다 (실제로 겪음: 40경기 이상 던진 마무리 시즌의 절반이 0승) |
| 불펜 승패비 | 마무리 0.80 · 불펜 1.02 · 선발 1.87 (실제 오승환 0.68 · 정우람 0.87) |
| 이닝 표기 | **3분의 1 단위**(.0/.1/.2)로 낸다 — `fmtIP()`. 계산은 실수로 하되 화면·문구에서만 바꾼다. `161.7이닝`은 야구에 없는 숫자다 |
| 출전 경기 | **입지별로 본다** — 간판 131 · 핵심/주전 128 · 준주전 83 · 백업 45 (최대 144, 전 경기 출장 나온다) |
| 경기 수 오독 주의 | 나이대로만 평균 내면 준주전·백업이 섞여 110경기처럼 보인다 (`games2.ts`로 입지별로 볼 것) |
| 홈런 | 파워 70 → 10 · 90 → 26 · 105 → 37 · 120 → 49 (실제 홈런왕 35~50) |
| 부문 타이틀 | **실제 KBO 15부문** — 타자 8(타율·안타·홈런·타점·득점·도루·출루율·장타율) · 투수 7(다승·평균자책점·탈삼진·세이브·홀드·승률·이닝) |
| 타이틀 기준선 | 실제 KBO 숫자가 아니라 **우리 리그 분포의 상위 12% 지점**에서 뽑는다 — 부문별 수상률 0.9~3.4% (`titlebar.ts`) |
| 국제대회 방식 | 실제 대회를 따른다 — WBC 조별 4경기+8강, 프리미어12 조별 5+슈퍼라운드, 올림픽 녹아웃·패자부활, AG 조별+슈퍼라운드 |
| 3·4위전 | WBC만 없다(2023년 폐지) — 3위에게 동메달을 주지 않는다 |
| 동기 | 드래프트 클래스 6명이 이름을 갖고 같이 늙는다 — 1군 안착 못 하고 사라짐 32% · 통산 WAR 중앙 11.2 · 29%가 타이틀 경험 |
| 동기의 힘 | **동기도 겨울마다 훈련한다** — 빼먹으면 플레이어만 자라 열 시즌이면 들러리가 된다 (WAR 중앙 2.2 · 내가 1위인 커리어 89%였다) |
| 최상위 동기 | 플레이어와 **같은 구분**(타자/투수)으로 만든다 — 다투는 부문이 겹쳐야 라이벌이다 |
| 타이틀 정산 | 부문 1위는 리그에 **한 명뿐**이다 — 나와 동기가 **같은 기준선**을 놓고 다투고 가장 나은 한 명만 가져간다(`settleTitles`) |
| 타이틀 기준선 공유 | 각자 다른 선을 뽑으면 둘 다 넘는 일이 거의 없어 경쟁이 안 일어난다 (실제로 겪음: 430번 중 1번만 뺏겼다) |
| 출장 배수 | **1을 넘기지 않는다** — 한 시즌은 144경기가 전부다 (1.05까지 허용했더니 선발이 232이닝을 던졌다) |
| 대표팀 | 타이틀 2회↑면 100% 발탁 · 포지션별 정원 반영(SP 6 · C 5 ↔ 1B 0 · DH −3) |
| 아시안게임 연령 제한 | 실제 KBO 규정 — **만 25세 이하 또는 프로 4년차 이하** + 나이 제한 없는 **와일드카드 3명**(발탁 중 10%, 실제 24명 중 3명 = 13%) |
| 올림픽·P12·WBC | **연령 제한 없다** — 야구는 축구와 달리 올림픽도 최정예로 나간다. 여기에 제한을 넣으면 실제와 어긋난다 |
| 월말 엔트리 | 1군↔2군 + **1군 안 보직 이동**(주전↔준주전↔백업) |
| 강등 조건 | 기량이 1군 기준 미달일 때만 — 주전·선발급은 한 달 부진으로 안 내려간다 |
| 강등 빈도 | 좋은 시즌 5% · 평범 11% · 부진 13% (시즌 중 2군 강등) |
| 연출 | 도입 타이핑 · 후보 카드 오픈 · 중계 전 로딩 화면 · 갈림길 모달 |
| 통보 라벨 길이 | 라벨 칸은 한 줄이다 — "훈련으로 얻은 것"처럼 길면 두 줄로 깨진다. **4글자 안**으로 쓴다(훈련 효과 · 나이 영향 · 구단 신뢰). 값이 길어 여러 줄이 되는 건 괜찮다(`items-start`) |
| 은퇴 뒤의 헤더 | 은퇴하면 입지 배지를 "은퇴"로 바꾸고 프로 연차를 더 세지 않는다 — "1군 필승조 · 프로 19년차"가 남아 있으면 아직 뛰는 사람으로 읽힌다 (실제로 겪음) |
| 칸을 건너뛴 모순 | 한 줄 안의 모순(`integrity`) 말고 **칸 사이**도 본다 — 우승인데 마지막 시리즈 패 · 8위인데 가을야구 · 현역 복무 해에 경기 기록(상무는 퓨처스에서 실제로 뛰므로 제외) · 3·4위전 없는 대회의 동메달 · 서비스타임 > 프로 시즌 수 (`scripts/logic.ts`) |
| 훈련과 노쇠는 따로 적는다 | **훈련은 능력치를 깎지 않는다** — `grow()`의 훈련 몫은 늘 0 이상이다. 합친 값 하나를 "훈련 결과"라고 적으면 서른다섯의 겨울이 "훈련 결과 −3"으로 떠, 훈련이 잘못한 것처럼 읽힌다. `grow()`가 `gains`(훈련이 보탠 몫)·`aging`(훈련이 없었다면 갔을 값)을 따로 돌려주고, 통보는 **훈련으로 얻은 것 / 나이로 잃은 것 / OVR 변화** 세 줄로 적는다. 스무 살의 하락은 나이 탓이 아니므로 "내려간 것"으로 말을 나눈다 (`scripts/campreport.ts`) |
| 훈련 몫을 재는 법 | **같은 주사위로** 훈련 없이 한 번 더 굴려 견준다(`land(dNat)`). 상한·반올림이 단조라 `훈련 포함 ≥ 훈련 제외`가 보장된다. 난수 호출 순서를 바꾸면 성장 곡선 전체가 다른 값이 되므로 `dNat`·`dTrain`으로 쪼갤 때 순서를 지킨다 |
| 캠프 뒤 OVR 낙폭 | 26세 이하 1% · 27~30세 14% · 31~34세 80% · 35세 이상 99%가 내려간다 — 나이가 들면 **내려가는 게 정상**이고, 화면은 그 원인을 나이로 지목해야 한다 |
| 훈련 결과 표기 | **오른 것만 보여주면 화면이 거짓말을 한다** — 서른다섯은 훈련이 잘 풀려도 노쇠가 상쇄해 총합이 0이 된다. 내려간 것도 같이 적고, 방어가 성과인 나이엔 그렇게 말한다 |
| 통보 오버레이 | 이적·협상·발탁·전역·상무 + **훈련 결과**(오프시즌마다 1회) — 중계 밖 사건만 |
| 엔트리 이동 | 중계가 **그 달 자리에 멈춰** 확인을 받는다 (오버레이로 중복 표시하지 않는다) |
| 1군 입지 분포 | 간판 5~15% / 핵심 11~29% / 주전 31~41% / 준주전 19~31% / 백업 9~12% |
| 올스타 | 주전 27% · 마무리 32% · 선발 24% · 간판 55% · 에이스 65% |
| 병역 | 상무 지원 2회 제한 · 합격 52% / 국제대회 면제 16% / 현역 34% |
| 복무 내용 | **방침 3택 + 부대 소식** — 복무는 버튼 하나였고 커리어 한가운데가 통째로 비어 있었다 (`military.ts`) |
| 복무 방침 | 상무: 실전/몸만들기/부대충실 · 현역: 몸만들기/복무충실/부대야구부. 전역 시 기량·신뢰·멘탈이 갈린다 |
| 복무의 값 | 상무(부대충실) −0.1 OVR · 현역(복무충실) −3.2 · 현역(몸만들기) −1.8 — 상무가 기량을 지키고, 현역에서도 방침이 손실을 반으로 줄인다 (`service2.ts`) |
| 부대 소식 | 상무 7종 · 현역 8종 — 혹한기 훈련 · 면회 · TV로 본 옛 동료 · 구단에서 온 편지 등 |
| 복무 기간 | 18개월 = **복무 시즌 1개 + 이듬해 후반기 복귀** (복귀 시즌 120~145타석) |
| 연봉 | 1군 시즌 중앙 2.8억 · 커리어 최고 중앙 9.8억 (상한 30억) |
| 인지도 | 순수 누적이 아니다 — 시즌이 닫힐 때 **지금 받아 마땅한 값** 쪽으로 42% 당긴다. 쌓은 업적은 바닥(최대 88)을 만들어 레전드는 잊히지 않는다 |
| 인지도 분포 | 은퇴 시 하위10% 38 · 중앙 67 · 상위10% 100 · 100 고정 12%. 통산 WAR 15~30 → 45 · 50+ → 89 (눈금이 실력을 구분한다) |
| 인지도 주의 | 누적만 시키면 **은퇴 시 중앙 98로 포화돼 정보량이 0**이 된다. 가산을 줄이는 게 아니라 목표값으로 당겨야 한다 (실제로 겪음) |
| 협상 결렬의 손해 | **구단 제시액 대비**로 잰다(−5~8%). 잘한 시즌엔 구단이 이미 큰 인상을 제시하므로, 작년 금액으로 되돌리면 성적이 좋을수록 손해가 커지는 거꾸로 된 구조가 된다 |
| 협상 문구의 조건 | "삭감 폭을 줄였다"는 **제시액보다 끌어올렸을 때만** 할 수 있는 말이다. 수용은 제시액을 그대로 받는 것이라 `next === offer`인데, 삭감 제안을 수용하면 이 가지에 걸려 "구단이 5억원까지 깎으려 했지만 5억원으로 막았습니다"가 됐다 (실제로 겪음). `scripts/negotext.ts` — **협상 선택지를 돌려가며** 재야 한다(push만 돌리면 수용 가지를 한 번도 안 탄다) |
| 협상 결렬의 바닥 | **구단이 인상을 제시한 해에는 작년 아래로 안 내려간다.** 삭감을 제시한 해에는 바닥이 없다 |
| 연봉 삭감 | 잘한 시즌(WAR 2.5+ 또는 타이틀)은 **절대 삭감 없음** · 부진 시즌만 23% 삭감 |
| 월별 컨디션 | 압도 13% · 상승 26% · 평범 43% · 부진 10% · 슬럼프 6% · 부상 1% |
| 이달의 선수 | 커리어당 4.7회 · 최다 18회 (한 시즌 반복 수상은 확률이 급감한다) |
| 승부처 장면 | **타자·투수 따로 쓴다** — 같이 쓰면 투수에게 "고교 시절의 한 타석"이 뜨고 선택지는 "병살을 유도한다"가 된다 (`scenefit.ts`) |
| 승부처 장면 수 | **178종** (타자 32 · 선발 22 · 불펜 18 · 2군 12+12 · 고교 10+10 · 대학 10+10 · 무대 21+21). 커리어당 54회 마주치는데 34종이 서로 다르다 — 같은 장면을 **1.6번**만 다시 본다 (93종일 때 2.2번 · 9종일 때 21번) |
| 반복을 재는 법 | `repeat.ts`의 `onStep`은 **한 상태를 여러 번 훑는다.** 그대로 세면 커리어당 209회 마주친 것으로 찍혀 반복 횟수가 통째로 부풀려진다(실제 54회). 그 해·그 달의 승부처를 **한 번만** 세도록 열쇠를 걸어야 한다 (실제로 겪음) |
| 무대 장면이 말하면 안 되는 것 | 올스타·국대·가을야구 장면은 **점수나 시리즈 전적을 못 박지 않는다** — "5점 차", "3승 2패"라고 써두면 옆에 붙는 결과 카드와 어긋난다. 분위기·상대·날씨만 말한다 |
| 이벤트 체인 | **13종** · 커리어당 8.4회 / 7.0종 (4종일 때는 4.7회 / 3.1종) |
| 자리는 한 방향으로만 흐르지 않는다 | 보직·포지션 전환이 **올라가는 쪽으로도** 있어야 한다. 예전엔 선발→불펜 하나뿐이라 중간계투로 시작하면 평생 중간계투였다 — 같은 기량인데 통산 WAR이 선발의 절반에 묶인다 |
| 투수 보직 전환 | 몸이 정한다 — **스태미나·제구가 되면 선발**(`starterFit`), **구속·멘탈이 되면 마무리**(`closerFit`). 유형 이름이 아니라 능력치를 본다(유형은 능력치에서 나오므로 돌아가는 길이다). 노쇠한 선발도 구위가 남았으면 불펜이 아니라 뒷문으로 간다 |
| 전환 통보가 약속하면 안 되는 것 | **자리를 옮기는 것과 9회를 맡는 것은 다르다.** 보직은 이듬해 캠프에서 기량으로 다시 정해지므로(`assignRole`), "마무리 전환 — 9회를 맡습니다"라고 못 박으면 정작 필승조로 시작하는 해가 생긴다 (실제로 겪음: 서른일곱에 전환 통보가 뜨고 그해 보직은 필승조였다). "뒷문 쪽으로 간다 · 9회는 기량으로 따낸다"로 적는다 |
| 수비 스펙트럼 | C → SS → 2B·3B·CF → LF·RF → 1B → DH 순으로 한 칸씩 내려간다. **포수는 넣지 않는다** — 안방은 아무나 볼 수 없어 수비가 떨어져도 마스크를 계속 쓴다 |
| 거슬러 올라가는 전환 | 발이 빠른 젊은 선수(주력 78+ · 수비 70+ · 28세 이하)는 코너·1루·지명에서 **중견수로 올라간다**(`speedShift`). 포지션 가치가 올라 같은 방망이로 더 쳐준다 |
| 전환 실측 | 중간계투 시작 → 유지 45% · 마무리 35% · 선발 20%, 통산 WAR 17.3 → **27.2**. 선발 시작 → 유지 50% · 불펜 38% · 마무리 12%. 마무리 시작 → 100% 유지. 포수 100% 유지 · 1루수 → 지명 70% · 중견 15%(발 전환). **이 값은 제안을 늘 수락했을 때다** — 거절하면 자리를 지킨다 (`scripts/roleswitch.ts`) |
| 승부처 | 커리어당 50회 — 1군 월별 27.6 · 가을야구 8.0 · 2군 6.1 · 국대 4.1 · 올스타 3.8 · 고교/대학 1.0 |
| 승부처 무게 | 무대가 클수록 인지도가 크게 움직인다 (국대 ×2 · 가을 ×1.8 · 올스타 ×1.6 · 1군 ×1 · 2군 ×0.35 · 아마 ×0.5) |
| 승부처 시점 | 반기 계산 → `HALF_REVIEW`(중계·선택) → `FINISH_HALF`(올스타·순위 판정) |
| 승부처 균형 | 거포↔초구노림 / 교타자↔맞혀내기 / 파워투수↔정면승부 / 제구투수↔코너 |
| 승부처 문구 ↔ 경기 결과 | **승패는 이미 정해져 있고 승부처만 나중에 뽑는다** — 둘을 따로 두면 "끝내기 만루홈런" 밑에 "패배 3-7"이 찍힌다. 올스타·국대·가을야구는 결과 카드가 바로 옆에 붙으므로 `rollStageClutch`에 그 경기의 승패를 넘기고, `fitOutcome()`이 문구만 바꾼다(기록은 그대로 → 밸런스 불변). `scripts/clutchwin.ts` 모순 0 |
| 승부처 장면과 그 달의 자리 | 장면은 **심을 때 그 달의 자리로 다시 맞춘다**(`fitClutchScene`). 반기가 시작될 때 걸어두므로, 그 사이 콜업·말소·보직 변경이 일어나면 8월에 1군으로 올라간 선수에게 10월 "퓨처스 올스타 선발 · 2군의 간판"이 뜬다 (실제로 겪음). 선발↔불펜도 같다 |
| 장면이 만드는 없던 과거 | 장면에 `requires`·`when`을 단다 — 1군에 올라간 적 없는 신인의 "강등 첫 경기(어제까지 1군이었습니다)", 한 팀에서만 뛴 선수의 "친정팀 상대", 안 다친 해의 "재활 마지막 날", 9월에 뜨는 "개막전", 4월에 뜨는 "시즌 최종전". 조건을 만족하는 장면이 없으면 조건 없는 장면으로 돌아간다 — **걸러내다 지워버리지 않았는지**까지 잰다 (`scripts/clutchseat.ts`) |
| 대회 현황판 | 현황판은 **지금 카드보다 앞서 가지 않는다.** 지금 경기까지 세면 "조별리그 1승 0패"를 읽고 나서 그 1차전 카드를 보게 된다 (실제로 겪음). `done`(공개된 경기 수)과 `current`(지금 카드의 경기)를 나눠 넘기고, 치르는 중인 경기는 승패 색 대신 흰 점으로 둔다 |
| 중계 배지의 뜻 | `Live` 옆 "1군 주전"은 **지금 보고 있는 달의 자리**다. `g.seasonLevel`은 반기가 다 끝난 뒤의 소속이라, 그걸 쓰면 10월에 내려간 선수가 중계 내내 "2군"으로 찍힌다 |
| 승부처와 출장 기록 | 승부처는 **뛴 경기에만 건다.** 달을 먼저 정하고 그 달은 나중에 시뮬레이션하므로, 부상으로 비는 달에 걸리면 "5월 9회말 2사 만루"가 뜨고 5월 출장은 0경기로 찍힌다. 월별은 `monthAvail`을 보고 고르고, 그래도 0경기면 뛰는 다음 달로 미룬다(끝까지 없으면 없던 일). 국제대회는 마지막 경기가 아니라 **실제로 나간 마지막 경기**에 건다 — `clutchGameIndex()` 한 곳에서 고르고 중계·기록·승패가 같은 경기를 본다(투수는 등판 안 하는 경기가 있어 35%가 어긋났다). `scripts/clutchgame.ts` 0건 |
| "끝내기"를 쓸 수 있는 자리 | 장면의 `walkoff` 표시가 있을 때만 — 7회 1사 만루에 "끝내기"가 뜨면 장면과 결과가 따로 논다. 무대 승부처(올스타·국대·가을)는 결과 카드가 승패를 말하므로 `walkoff: false`다 |
| FA | 자격 도달 중앙 29세 · 연평균÷직전연봉 중앙 4.2배 (실제 KBO 2.7~4.4배) |
| FA 등급 | 직전 연봉으로 A(5억+)·B(2.5억+)·C — 등급이 높을수록 보상이 무거워 **붙는 구단이 줄어든다** (A 3곳 · B 4곳 · C 6곳) |
| FA 제안 | 구단마다 **모양이 다르다** — 전액 보장 31% · 장기 안정 28% · 옵션 승부 24% · 단기 고액 17% |
| FA 편차 | 한 협상 안에서 총액 최대·최저가 59% 차이 · 연수 폭 2.8년 · 옵션 0~34% |
| 서비스타임 | **1군 등록 비율만** 센다 — 가동률을 또 곱하지 않는다(이중 차감) |

| 명예의 전당 | 레전드 100%(1차) · 명전급 89%(평균 3.2차) · 그 아래 0% |

### 측정 스크립트

`audit.ts`(전체 감사) 외에 `transfersweep`(이적), `salary`(연봉), `goals`(시즌 목표),
`roles`(입지), `styles`(선수 유형), `draft`(진로), `games`(경기 수), `legacy`(은퇴 후),
`statdist`(기록 분포), `intlline`(국제대회·올스타 기록), `fa`(FA 규모),
`allstar`(보직별 선정률), `potcap`(나이별 성장), `totals`(기록 정합성),
`draftscore`(고교 성적 ↔ 지명 예상), `rookieyear`(신인 첫 시즌 소속),
`rookiestat`(신인 성적·훈련 상승폭), `sangmu`(상무 지원), `notices`(통보 발생),
`hellcheck`(지옥 훈련 도박 균형), `transferodds`(표기 확률 ↔ 실제 성사율),
`service`(복무 기간·복귀 시점), `school`(학교 전력 효과),
`titles`(성적 대비 수상), `titlebar`(부문별 기준선·수상률), `nego`(협상 도박 균형), `negotext`(협상 통보 문구), `integrity`(기록 불변식), `valuecheck`(값의 범위), `goaltext`(목표 표기↔판정), `campreport`(훈련 몫 ↔ 나이 몫), `logic`(칸 사이의 모순), `ceiling`(천장·훈련 몫), `headroom`(성장 여지), `traincover`(훈련 방향의 덮개), `injurybal`(부상 빈도·무게), `earlygame`(초반 여덟 시즌), `rolebar`(OVR ↔ 보직), `fair`(자리만 바꿨을 때의 격차), `stylewar`(같은 OVR에서 유형의 값), `styledrift`(유형 격차가 어디서 오는가), `roleswitch`(보직·포지션 전환), `traingrade`(훈련 성과 분포), `agency`(선택의 무게), `decisions`(결정 밀도), `repeat`(장면 반복), `oddscheck`(표기 확률 검수), `rivals`(동기 분포·타이틀 중복), `fame`(인지도 눈금), `leagueavg`(리그 평균 기준), `injurymonth`(부상의 달력 배치), `wish`(희망 구단의 무게), `park`(구장 효과 크기), `scenefit`(장면↔자리 일치), `service2`(복무 방침의 값), `wl`(투수 승패), `randomcreate`(랜덤 조합 정합성), `agelimit`(AG 연령 제한), `wl`(투수 승패), `ipfmt`(이닝 표기), `titlebar`(부문별 기준선), `asscore`(올스타 스코어↔승패), `asorder`(올스타 정보 유출), `textcheck`(문구 빈칸),
`service_fa`(서비스타임 ↔ FA 도달), `titlemark`(타이틀↔기록 대응),
`monthform`(월별 단계 분포·이달의 선수), `mvppay`(성적 대비 제시액),
`clutch`(승부처 선택지 균형), `clutchwin`(승부처 문구 ↔ 경기 결과), `clutchgame`(승부처 ↔ 출장 기록), `clutchseat`(장면 ↔ 그 달의 자리), `fagate`(FA를 막는 조건),
`aging2`·`aging3`(나이별 기록·OVR 낙폭), `refused`(은퇴 권고 거부 후 협상),
`games2`(입지별 출전 경기)를 쓴다.

## 문서

- `docs/ARCHITECTURE.md` — 프로젝트 구조도 (모듈 관계·시즌 흐름·시뮬레이션 방식)
- `README.md` — 게임 시스템 설명 (플레이어·개발자용)
- `docs/HISTORY.md` — 개발 이력, 결정 이유, 발견한 버그
